export type ProfileEditForm  = {
    id: string,
    name: string | null,
    username: string | null,
    email: string,
    password:  string | null,
    image: string | null,
    bio: string | null
}